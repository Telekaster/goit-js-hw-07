const categoryCount = document.querySelectorAll('.item').length;
// console.log('Всего категорий', categoryCount);

const categoryFirstTitle = document.querySelector('h2').textContent;
// --------------------------------------------------------------------

const mark = document.querySelector('.item').childNodes[2];
console.log(mark);



// --------------------------------------------------------------------
const categoryFirstCount = document.querySelectorAll('.item')
// console.log(categoryFirstCount);
// console.log(`Первая категория - название: '${categoryFirstTitle}', количество элементов: ${categoryFirstCount}`);